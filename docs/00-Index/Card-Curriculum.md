# Telegram Daily Cards Curriculum (30min)

规则：每天 1 张卡（CN 60s + EN 60s + 3 pitfalls + 1 handwrite snippet）。

## Week 1 — Core JS (mid-level CN interview)
1) Event loop: microtask vs macrotask
2) Promise combinators: all / allSettled / race / any
3) this binding rules
4) Closures & scope (incl. var loop)
5) Type coercion (== vs === + gotchas)
6) async/await mental model + error propagation
7) prototype chain (explain clearly, no deep dive)

## Week 2 — React core
8) Why re-render? rendering triggers
9) keys & reconciliation
10) hooks deps & stale closure
11) useMemo/useCallback tradeoffs
12) Context performance pitfalls

## Week 3 — Browser/Network basics
13) rendering pipeline (layout/paint/composite)
14) HTTP caching (Cache-Control/ETag)
15) CORS & preflight
16) XSS/CSRF/CSP (practical)
